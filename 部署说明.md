# Windows 一键部署说明

这个目录里新增了一套通用部署器。它不会修改业务代码，主要用于把当前包复制到一台新的 Windows 电脑后完成依赖检查、安装、启动和停止。

## 最快使用方式

1. 把整个项目文件夹复制到新电脑，不要只复制 `deploy` 文件夹。
2. 双击根目录的 `deploy.bat`。
3. 等待脚本完成。看到 `Deployment completed` 后，应用已按配置启动。
4. 如果 Windows 防火墙弹出提示，只勾选需要访问该应用的网络类型，然后允许。

部署日志位于：

```text
.deploy\deploy.log
.deploy\app.out.log
.deploy\app.err.log
```

## 可用的双击入口

| 文件 | 作用 |
| --- | --- |
| `deploy.bat` | 检查环境、安装依赖、保存启动配置，然后启动应用 |
| `start-application.bat` | 启动已经部署过的应用 |
| `stop-application.bat` | 停止应用 |
| `application-status.bat` | 查看进程和本地健康检查状态 |
| `environment-check.bat` | 检查 Python、Node.js、Docker、Git、winget 等环境 |

## 脚本会自动识别什么

部署器默认使用 `Mode = "Auto"`，检测顺序如下：

1. 有 `compose.yaml`、`docker-compose.yml` 时使用 Docker。
2. 有 `package.json` 时使用 Node.js。
3. 有 `requirements.txt`、`pyproject.toml`、`setup.py` 或 `Pipfile` 时使用 Python。
4. 有 `index.html` 时按静态网页启动。
5. 以上都没有时，需要在 `deploy\deploy.config.ps1` 中设置自定义启动命令。

Python 项目会创建 `.venv`，Node.js 项目会执行依赖安装；只要 `package.json` 中存在 `build` 脚本，默认还会执行构建。

## 需要修改配置时

编辑 `deploy\deploy.config.ps1`。几个最常用的配置如下：

```powershell
@{
    AppName = "kechuang"
    Mode = "Auto"
    InstallDependencies = $true
    AutoInstallRuntimes = $true
    Offline = $false
    RunBuild = $true
    StartAfterDeploy = $true

    Url = "http://127.0.0.1:8000"
    StartExecutable = ""
    StartArguments = @()
}
```

如果项目同时包含前后端，自动识别可能无法判断应该启动哪一个。这时请明确指定，例如：

```powershell
@{
    AppName = "kechuang"
    Mode = "Python"
    StartExecutable = ".venv\Scripts\python.exe"
    StartArguments = @("app.py")
    Url = "http://127.0.0.1:8000"
}
```

如果是 Node.js 项目：

```powershell
@{
    AppName = "kechuang"
    Mode = "Node"
    StartExecutable = "npm.cmd"
    StartArguments = @("run", "start")
    Url = "http://127.0.0.1:3000"
}
```

## 没有网络的新电脑

当前脚本默认通过 `winget` 自动安装缺失的 Python 3.11 或 Node.js LTS，所以新电脑需要能访问互联网。

如果新电脑离线，需要在打包时把便携运行时放进项目：

```text
runtime\
  python\
    python.exe
  node\
    node.exe
    npm.cmd
```

然后在 `deploy\deploy.config.ps1` 中设置：

```powershell
Offline = $true
PythonCommand = "runtime\python\python.exe"
NodeCommand = "runtime\node\node.exe"
```

Python 离线依赖可以放在下面任意一个目录，部署器会自动使用：

```text
wheelhouse\
wheels\
offline\wheels\
offline\wheelhouse\
```

离线包准备方式示例：

```powershell
python -m pip download -r requirements.txt -d wheelhouse
```

## 建议的交付目录

交付给新电脑前，目录建议保持为：

```text
kechuang\
  ...项目原文件...
  deploy\
  deploy.bat
  start-application.bat
  stop-application.bat
  application-status.bat
  environment-check.bat
  部署说明.md
```

不要把已经在本机生成的 `.venv`、`node_modules` 作为唯一依赖来源交付。新电脑的路径和系统架构可能不同，部署器会在新电脑上重新生成这些目录。

## 常见问题

### 双击后窗口立即消失

请从 `CMD` 或 PowerShell 进入项目目录，直接运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\deploy\deploy.ps1 -Action Deploy
```

这样可以看到完整错误。部署器本身也会把大部分输出写入 `.deploy\deploy.log`。

### 提示 Python、Node.js 或 Docker 找不到

先双击 `environment-check.bat`。允许联网时，脚本会尝试通过 `winget` 安装 Python 或 Node.js；Docker Desktop 通常需要单独安装并重启电脑。

### 应用启动了，但浏览器打不开

检查 `deploy\deploy.config.ps1` 中的 `Url` 是否与实际端口一致。对于 Flask、FastAPI、Django、Vite 等项目，端口可能不是默认的 `8000`。

### 需要停止应用

双击 `stop-application.bat`。脚本会按照部署时记录的模式停止进程或 Docker 服务。
